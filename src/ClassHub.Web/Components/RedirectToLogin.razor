@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo($"/Account/Login?returnUrl={Uri.EscapeDataString(Nav.ToBaseRelativePath(Nav.Uri))}", forceLoad: true);
    }
}
